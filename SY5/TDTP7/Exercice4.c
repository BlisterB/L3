#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <wait.h>

void recherche(int* t, int taille_tab){
	int tail;
	while(	
}

int main(void){
	int i, n = 1000;
	int t[n];
	for(i = 0; i < n; i++){
		t[i] = i;
	}
	recherche(t, n);
		
	return 1;
}
